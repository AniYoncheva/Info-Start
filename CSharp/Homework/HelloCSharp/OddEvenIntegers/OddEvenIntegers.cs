using System;

//Write an expression that checks if given doubleeger is odd or even. Examples:

    class OddEvendoubleegers
    {
        static void Main()
        {
            int n = int.Parse(Console.ReadLine());
            bool isEvent = n % 2 == 0;
            Console.WriteLine(isEvent);
        }
    }

